# 2.2.2: models/TimeLLM.py patches torch.compiler.is_compiling for transformers 4.40 + bitsandbytes.
# Alternatively install torch>=2.3 (cu121 wheels) and the patch is a no-op.
torch==2.2.2
# bitsandbytes>=0.43 expects torch._C._has_xpu (PyTorch 2.3+). Pin for torch 2.2.2 or upgrade torch.
bitsandbytes>=0.41.0,<0.43
einops==0.7.0
matplotlib==3.7.0
numpy==1.23.5
pandas==1.5.3
scikit_learn==1.2.2
scipy==1.12.0
tqdm==4.65.0
# 0.4.x calls F.linear on 4bit base weights (Half vs Byte). Use >=0.10 for Qwen2 BNB4 + LoRA inference.
peft>=0.10.0,<0.14
# Qwen2 / Qwen2-7B-Instruct needs transformers >= 4.37; 4.31 raises KeyError: 'qwen2'
accelerate==0.30.1
transformers==4.40.2
deepspeed==0.14.0
sentencepiece==0.2.0
